import { Props } from '@storybook/addon-docs/blocks';
import { RemoteVideos } from './';

<Meta title="SDK Components/RemoteVideos" />

# RemoteVideos

Renders all remote attendee's videos with no layout. You will need to provide the styling to handle the layout.

Remote videos are added to the session as attendees start and stop their own video.

## Importing

```javascript
import { RemoteVideos } from 'amazon-chime-sdk-component-library-react';
```

## Usage

```jsx
import React from 'react';
import { MeetingProvider, RemoteVideos } from 'amazon-chime-sdk-component-library-react';

const App = () => (
  <MeetingProvider>
    <MyCustomGrid>
      <RemoteVideos />
    </MyCustomGrid>
  </MeetingProvider>
);
```

## Props
<Props of={RemoteVideos} />