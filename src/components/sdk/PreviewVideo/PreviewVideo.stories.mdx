import { Props } from '@storybook/addon-docs/blocks';
import { PreviewVideo } from './';

<Meta title="SDK Components/PreviewVideo" />

# PreviewVideo

The `PreviewVideo` component renders a preview of the local video. If there is no available video input, the video will be blank. 

A `PreviewVideo` is typically used on a pre-meeting view, or a device settings page. It doesn not effect the local video state.

## Importing

```javascript
import { PreviewVideo } from 'amazon-chime-sdk-component-library-react';
```

## Usage

```jsx
import React from 'react';
import {
  MeetingProvider,
  PreviewVideo
} from 'amazon-chime-sdk-component-library-react';

const App = () => {
  return (
    <MeetingProvider>
      <PreviewVideo />
    </MeetingProvider>
  );
};
```

## Props

<Props of={PreviewVideo} />
