import { Preview, Props } from '@storybook/addon-docs/blocks';
import { ThemeProvider } from 'styled-components';

import Pause from './';
import { lightTheme } from '../../../../theme/';

# Pause Icon

The Pause component creates an pause icon using inline SVG. Typically, you can use it to represent a pause action.

## Importing

```javascript
import { Pause } from 'amazon-chime-sdk-component-library-react';
```

## Example

<ThemeProvider theme={lightTheme}>
  <Preview>
    <Pause width="2rem" />
  </Preview>
</ThemeProvider>

```jsx
<Pause width="2rem" />
```

## Props

<Props of={Pause} />
