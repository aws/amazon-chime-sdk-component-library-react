import { Preview, Props } from '@storybook/addon-docs/blocks';
import { ThemeProvider } from 'styled-components';

import ContentShare from './';
import { lightTheme } from '../../../../theme/';

# ContentShare Icon

The ContentShare component creates a display icon using inline SVG. This icon is typically used to represent a screen share action.

## Importing

```javascript
import { ContentShare } from 'amazon-chime-sdk-component-library-react';
```

## Example

<ThemeProvider theme={lightTheme}>
  <Preview>
    <ContentShare width="2rem" />
  </Preview>
</ThemeProvider>

```jsx
<ContentShare width="2rem" />
```

## Props

<Props of={ContentShare} />
