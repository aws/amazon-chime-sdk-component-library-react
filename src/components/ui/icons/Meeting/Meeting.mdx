import { Preview, Props } from '@storybook/addon-docs/blocks';
import { ThemeProvider } from 'styled-components';

import Meeting from './';
import { lightTheme } from '../../../../theme/';

# Meeting Icon

The Meeting component creates a table icon using inline SVG. This icon is typically used to show meeting settings or options.

## Importing

```javascript
import { Meeting } from 'amazon-chime-sdk-component-library-react';
```

## Example

<ThemeProvider theme={lightTheme}>
  <Preview>
    <Meeting width="2rem" />
  </Preview>
</ThemeProvider>

```jsx
<Meeting width="2rem" />
```

## Props

<Props of={Meeting} />
