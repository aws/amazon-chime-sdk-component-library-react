import { Preview, Props } from '@storybook/addon-docs/blocks';
import { ThemeProvider } from 'styled-components';

import Hamburger from './';
import { lightTheme } from '../../../../theme/';

# Hamburger Icon

The Hamburger component creates an icon with three strips using inline SVG. Typically, you can use it for a menu.

## Importing

```javascript
import { Hamburger } from 'amazon-chime-sdk-component-library-react';
```

## Example

<ThemeProvider theme={lightTheme}>
  <Preview>
    <Hamburger width="2rem" />
  </Preview>
</ThemeProvider>

```jsx
<Hamburger width="2rem" />
```

## Props

<Props of={Hamburger} />
