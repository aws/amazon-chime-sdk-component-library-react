import { Canvas, Meta, ArgTypes } from '@storybook/blocks';
import { ThemeProvider } from 'styled-components';

import Like from './';
import { lightTheme } from '../../../../theme/';
import { GlobalStyles } from '../../../../theme/GlobalStyles';
import * as LikeStories from './Like.stories.tsx';

<Meta of={LikeStories} />

# Like Icon

The Like component creates an icon using inline SVG. Typically, you can use it to indicate a like action.

## Importing

```javascript
import { Like } from 'amazon-chime-sdk-component-library-react';
```

## Example

<ThemeProvider theme={lightTheme}>
  <GlobalStyles />
  <Canvas>
    <Like width="2rem" />
  </Canvas>
</ThemeProvider>

```jsx
<Like width="2rem" />
```

## Props

<ArgTypes of={Like} />
