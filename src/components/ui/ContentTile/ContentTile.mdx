import { Canvas, Meta, ArgTypes } from '@storybook/blocks';
import { ThemeProvider } from 'styled-components';

import ContentTile from './';
import { lightTheme } from '../../../theme/';
import { GlobalStyles } from '../../../theme/GlobalStyles';
import * as ContentTileStories from './ContentTile.stories.tsx';

<Meta of={ContentTileStories} />

# ContentTile

You can use the ContentTile component to render content share stream.

## Importing

```javascript
import { ContentTile } from 'amazon-chime-sdk-component-library-react';
```

## Example

<ThemeProvider theme={lightTheme}>
  <GlobalStyles />
  <Canvas>
    <div style={{ height: '15rem', width: '25rem', margin: '2rem auto' }}>
      <ContentTile nameplate="Content share" />
    </div>
  </Canvas>
</ThemeProvider>

```jsx
<ContentTile nameplate="Content share" />
```

## Props

<ArgTypes of={ContentTile} />
